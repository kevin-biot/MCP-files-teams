apiVersion: apps/v1
kind: Deployment
metadata:
  name: mcp-server
spec:
  template:
    spec:
      containers:
        - name: mcp
          image: image-registry.openshift-image-registry.svc:5000/team-memory-dev/mcp-server:latest
          imagePullPolicy: Always

