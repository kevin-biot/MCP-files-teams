apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-only-oauth2-proxy-to-chroma
spec:
  podSelector:
    matchLabels:
      app: chroma
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app: oauth2-proxy
      ports:
        - protocol: TCP
          port: 8000
